# Lessons Log
_Updated by agent after each user correction. Read at session start._

---

### General Rules (Always Apply)

**Rule:** Always normalize string inputs to lowercase at boundaries.

**Rule:** Never trust UI constraints alone; validate again on the server.

**Rule:** If a browser test is flaky because the UI correctly prevents bad input, replace it with deterministic unit coverage.

**Rule:** Do not overbuild before the first vertical slice works end to end.
